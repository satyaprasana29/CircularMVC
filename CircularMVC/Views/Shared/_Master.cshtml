<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
</head>
<body>
    <div>
        @Html.ActionLink("Register", "AddEmployees", "Index");
        @Html.ActionLink("Login", "Login", "Index");
        
        @RenderBody()
    </div>
</body>
</html>
